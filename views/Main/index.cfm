<cfoutput>
    <div ng-controller="MainIndex"> 
        <a href="#event.buildLink('login')#">Login</a>
       <!--- <div ng-repeat = "ins in insList">
            {{ins.name}} &nbsp;&nbsp;{{ins.title}}<br>
        </div> --->
    </div>
</cfoutput>